# AI Issue Demo

This repository is used to test an AI system that converts GitHub Issues into Pull Requests.

The project intentionally contains bugs so that issues can be created and resolved automatically.